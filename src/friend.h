#pragma once


// This is the shared global variable friend file.
// Create an instance of the Friend class when you need to change
// widely used variables like the width and height, color,
// timeline and border parameters, etc


class Friend
{
public:
	int width = 1024;
	int height = 768;
};